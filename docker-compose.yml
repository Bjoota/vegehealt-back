version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3000"  
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - PORT=3000
    command: npm run dev